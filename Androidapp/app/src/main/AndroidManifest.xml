<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZKKYCWallet">

        <!-- Splash Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Loading Screen -->
        <activity
            android:name=".LoadingScreenActivity"
            android:exported="false" />

        <!-- PIN Setup Screen -->
        <activity
            android:name=".CreatePinActivity"
            android:exported="false" />

        <!-- PIN Enter Screen -->
        <activity
            android:name=".EnterPinActivity"
            android:exported="false" />

        <!-- Onboarding Screen -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- Aadhaar Import Screen -->
        <activity
            android:name=".AadhaarImportActivity"
            android:exported="false" />

        <!-- Proof Request Screen -->
        <activity
            android:name=".ProofRequestActivity"
            android:exported="false" />

        <!-- Proof Result Screen -->
        <activity
            android:name=".ProofResultActivity"
            android:exported="false" />

    </application>

</manifest>
